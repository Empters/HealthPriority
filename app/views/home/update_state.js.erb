var states_select = $("#states_select");
if (states_select) {
    states_select.html("<%= escape_javascript(options_for_select(@states.collect { |p| [p.name, p.id] }, {:prompt => "* #{t('state')}"})) %>");
}